# userDashboard Changelog

## 8.8.1

### New Features

* Use fallback textarea when ckeditor is not available (#82)

### Bug Fixes

* Escape stored string values rendered by the dashboard profile, site listing and content list views

* Keep the files browser within the current user's folder and escape the displayed path
